#include "holberton.h"

/**
 * mul - Makes the multiple of two numbers
 * @a: First operand
 * @b: Second operand
 *
 * Return: The multiple of two numbers.
 */
int mul(int a, int b)
{
	int c;

	c = (a * b);

	return (c);
}
