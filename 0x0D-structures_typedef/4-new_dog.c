#include <stdio.h>
#include "dog.h"

/**
 * new_dog - Not sure what this does.
 * @name: Dog name
 * @age: Dog age
 * @owner: Owner name
 * Return: ?
 */
dog_t *new_dog(char *name, float age, char *owner)
{


	if (new_dog == 0)
		return (NULL);

	new_dog->name = name;
	new_dog->age = age;
	new_dog->owner = owner;
}
